describe('lto-chain-listener', () => {
  // @todo: make these tests
  describe('start()', () => {
    test('should log that listener is starting', async () => {});

    test('should not start if it has already started', async () => {});

    test('should start from the last block', async () => {});

    test('should start from the starting block parameter', async () => {});

    test('should catch and log errors', async () => {});
  });
});
